# WS-05 客户端产品化

- 目标：将模块化客户端接到真实 API，完善发布、报名、选人、订单、消息和交付体验。
- 拥有：`src/client/**`（WS-01 完成后移交）及客户端测试；不修改共享契约。
- 依赖：WS-01、相应服务端 API。
- 交付：加载/错误/空状态、乐观并发冲突提示、会话过期、响应式布局和可访问性。
- 约束：不能用前端隐藏代替权限；不能伪造付款成功；本地演示与真实 API 模式要有明显环境标识。
- 验收：桌面和 390px 手机端关键流程、键盘操作、网络失败与 409 冲突场景通过。
