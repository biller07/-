# WS-04 订单消息与通知

- 目标：入选后双方可跨设备查看和发送订单消息，并产生站内通知。
- 拥有：`src/server/modules/chat/**`、`src/server/modules/notifications/**` 及专属测试。
- 依赖：WS-02、WS-03 的 Assignment。
- 关键约束：只有发布方和已选协作者可访问；发送操作持久化后再推送；WebSocket 断线不丢历史。
- 首版不做：任意私聊、群聊、端到端加密、读取学校账号内容。
- 验收：越权读取/发送拒绝、消息顺序稳定、断线重连和分页测试通过。
