# 修改确认说明

“修改确认”是实现提交之后、合并主分支之前的强制门禁。它让项目所有者知道谁改了什么、测试是否通过、从哪个提交可以回退。

1. 开发者完成开发，把任务改为“待核对”。
2. 核对通过后创建实现提交，提交格式为 `[任务号][修改人编号] 中文摘要`。
3. 用实现提交哈希创建确认记录，把任务改为“待确认”。
4. 项目所有者在确认记录中填写确认结果；确认前不得合并到 `main`。
5. 合并后记录合并提交，并把任务改为“已完成”。

确认不是口头状态：必须有版本库内的确认文件，且文件中包含实现提交和回退方法。
