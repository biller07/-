# 核对报告：T-001

- 任务号：T-001
- 修改人编号：P-001
- 对话编号：C-20260821-001
- 分支：`work/T-001-chinese-governance`
- 核对日期：2026-08-21
- 核对状态：通过

## 修改范围核对

- 计划内文件：根目录协作入口、开发协作文档、协作记录、治理脚本、浏览器测试路径和 `.gitignore`。
- 实际修改：将可中文化的开发目录和文件迁移为中文名称；建立人员、对话、任务、认领、核对、确认、交接和提交记录；保留 `.agents`、`AGENTS.md`、`README.md` 及程序入口等工具固定名称。
- 越界修改：未发现。产品业务代码没有发生功能修改。
- 旧路径引用：仓库当前内容中未发现已停用的英文协作目录、旧任务板入口或旧截图目录引用。
- `git diff --check`：通过；仅显示 Windows 将来可能把 LF 转成 CRLF 的提示，无空白错误。

## 验收与测试

| 命令或检查 | 结果 | 说明 |
|---|---|---|
| `node --check app.js` | 通过 | 原型主脚本语法有效 |
| `node --check "测试/浏览器全流程测试.js"` | 通过 | 改名后的测试脚本语法有效 |
| `python -m json.tool "协作记录/任务进度/任务板.json"` | 通过 | 任务板 JSON 可解析 |
| `python -m json.tool "协作记录/任务进度/工作流.json"` | 通过 | 工作流 JSON 可解析 |
| `powershell -ExecutionPolicy Bypass -File "脚本/校验开发治理.ps1"` | 通过 | 26 个必需文件、10 条工作流、13 个任务、1 个修改人、1 个有效认领均通过；真实支付关闭 |
| `node "测试/浏览器全流程测试.js"` | 通过 | 发布无资金冻结；跨账号报名、选人、对话、交付、验收和演示付款通过；390px 手机布局通过 |
| 本地服务器退出检查 | 通过 | 浏览器测试结束后已停止进程 262896 |

## 发现并修正的问题

首次用 Windows PowerShell 5.1 运行治理脚本时，中文源文件因缺少 UTF-8 BOM 被误读。已把脚本机械转换为 UTF-8 BOM，再次运行后全部通过。这个失败没有被隐藏，最终结果以修正后的复验为准。

## 风险和已知限制

- 中文路径依赖 Git 和编辑器使用 UTF-8；固定工具目录 `.agents` 和程序技术入口继续保留英文。
- 当前核对验证的是交互原型和协作治理，不代表真实服务端、多用户同步、合规审查或真实支付已完成。
- 真实支付开关仍为 `false`。

## 核对结论

计划内验收全部通过，允许创建 T-001 实现提交。实现提交产生后必须新增修改确认记录，并保持任务为“待确认”，直到项目所有者明确确认。
