# “算力集”产品方案（原型版）

## 1. 产品是什么

“算力集”是一个以作业辅导与协作完成为主要入口、同时允许其他真实任务发布的服务撮合平台。用户不被永久划分为甲方或乙方：同一个账号既可以发布需求，也可以报名自己擅长的订单。

第一阶段主推作业思路辅导、代码调试、步骤讲解、错题修改和共同完成；同时保留编程开发、数据处理、模型训练、AI 内容和算力等分类。平台的核心价值不是简单帮双方认识，而是让发布方先看到候选人的初版思路或小样，再把选人、沟通、交付、验收和结算沉淀在一条可追溯流程中。

一句话定位：**先看初版能力，再选择合适的人把任务一起完成。**

## 2. 原型中已经验证的主流程

```text
登录 / 切换身份
  ├─ 发布需求 ── 等待多人初版报名 ── 比较思路/小样/报价/时间 ── 选定一人
  └─ 浏览需求 ── 提交初版报名 ───────────────────────────┘
                                                        │
                                           订单内对话与共同确认
                                                        │
                                           提交交付物 ── 发布方验收
                                                        ├─ 退回修改 ── 再次提交
                                                        └─ 验收通过 ── 演示付款 ── 完成
```

原型已经采用“多人报名、发布方选定一人”的模式。每位候选人提交初步计划、初版内容或小样、报价和预计时间；发布方选定后，其他报名自动结束，订单进入单接单方协作阶段。暂不做一单多人协作。

## 3. 建议的正式订单状态

| 状态 | 含义 | 允许的关键操作 |
|---|---|---|
| `draft` | 草稿 | 编辑、删除、提交审核 |
| `open` | 开放报名 | 提交/修改初版报名、比较候选人、下架 |
| `in_progress` | 已选人/协作中 | 订单内沟通、提交交付物、申请取消 |
| `submitted` | 待验收 | 验收、退回修改、发起争议 |
| `pending_payment` | 付款码订单待付款 | 展示收款码、确认付款 |
| `completed` | 已结算 | 评价、开票、归档 |
| `cancelled` | 已取消 | 查看退款记录 |
| `disputed` | 争议中 | 补充证据、等待平台处理 |

状态迁移必须由服务端控制并写入审计日志，不能只依赖前端按钮隐藏。

## 4. 首版页面范围

### 公共与账号

- 手机号/微信登录、实名认证、个人资料、能力标签；
- 发布与接取统一账号，不设置互斥身份；
- 信用评分、完成订单数、纠纷率和擅长领域。

### 悬赏广场

- “作业辅导”作为首个和默认分类，其他需求继续开放；
- 搜索、分类、预算和完成期限筛选；
- 详情包含需求、素材说明、交付标准、知识产权与保密要求；
- 所有开放需求采用“先报名、后选人”，不再使用谁先点击谁获得订单的抢单模式；
- 报名必须包含初步思路、初版内容或小样、报价和预计时间。

### 订单工作台

- 我发布的、我报名/接取的、待我处理；
- 发布方比较报名并选定协作者；
- 选人后开启订单内沟通、附件、交付版本、退回原因和验收记录；
- 超时提醒、取消申请、争议入口与双方评价。

### 结算演示

- 当前只保留完成后付款码和双方确认结算的交互演示；
- 不冻结真实资金，不把充值、提现或平台担保描述成已接通能力；
- 持牌支付服务、退款、分账和担保方案在完成协作流程验证后单独调研。

## 5. 最小数据模型

| 表 | 关键字段 |
|---|---|
| `users` | id, phone, display_name, kyc_status, rating, status |
| `user_skills` | user_id, skill_id, proficiency, proof_url |
| `tasks` | id, owner_id, title, category, budget, settlement_type, status, deadline, version |
| `applications` | id, task_id, applicant_id, proposal, sample, quote, estimated_days, status |
| `task_assignments` | task_id, provider_id, selected_application_id, accepted_at, cancelled_at |
| `deliveries` | id, task_id, provider_id, version, note, submitted_at |
| `delivery_files` | delivery_id, object_key, file_name, hash, scan_status |
| `acceptances` | task_id, result, note, operator_id, created_at |
| `wallet_accounts` | user_id, available, frozen, withdrawing, version |
| `wallet_entries` | id, user_id, order_id, direction, amount, type, balance_after, provider_ref |
| `payments` | id, order_id, channel, provider_trade_no, amount, status |
| `order_events` | order_id, actor_id, event_type, before_status, after_status, payload, created_at |
| `messages` | order_id, sender_id, content, attachment_id, created_at |
| `disputes` | order_id, initiator_id, reason, evidence, status, resolution |

报名一人一单只能有一条有效记录，可在选人前更新。选人必须用数据库事务同时完成：锁定订单、选中一份报名、拒绝其他报名、创建协作关系并写入订单事件。以后接入钱包时，金额使用整数“分”存储，资金流水只追加、不覆盖。

## 6. 生产技术架构建议

```text
响应式 Web / PWA（Next.js 或 Vue/Nuxt）
            │ HTTPS + REST/WebSocket
API 服务（NestJS / FastAPI）
   ├─ PostgreSQL：账号、需求、报名、订单、对话与审计事件
   ├─ Redis：会话、限流、任务锁、通知队列
   ├─ S3/OSS：需求附件与交付文件
   ├─ 消息服务：站内信、短信、微信通知
   └─ 持牌支付机构：后续接入支付、退款、分账与提现
```

原型阶段用一套响应式 PWA 同时覆盖电脑和手机最划算。市场验证成功后，再把高频能力做成微信小程序或原生 App；不建议一开始维护三套独立客户端。

## 7. 支付和合规的关键边界

- 平台不能用普通公司银行账户长期沉淀用户充值款；应接入具备相应能力的持牌支付机构，并使用其分账/担保类产品；
- “平台余额”在产品上是账本展示，真实资金应由支付机构托管；
- 提现必须包含实名一致性、限额、反洗钱、风控审核和失败回退；
- 订单需明确知识产权归属、素材授权、保密义务、退款条件和争议规则；
- 作业类需求应以辅导、讲解、共同完成、批改和复习为主，禁止代考、冒名提交、论文代写以及违反学校规则的服务；
- AI 生成内容要有内容审核、敏感数据处理和侵权投诉通道；
- 高风险或不适合平台化的任务应设置禁售清单。

原型里的充值、提现、二维码和结算全部是本地模拟，不代表这些生产能力已经接通。

## 8. MVP 优先顺序

### 阶段 A：封闭验证（2–3 周）

以“作业辅导与共同完成”为主要场景，邀请 20–50 个真实用户，同时允许少量其他类型需求。运营人工审核作业边界，结算先用明确标注的演示或合规第三方方式。重点验证用户是否愿意提交初版、发布方是否能据此选人，以及选人后能否顺利沟通和验收。

### 阶段 B：可交易 MVP（4–8 周）

实现服务端账号、需求、多人报名、发布方选人、订单状态机、附件、站内沟通和基础运营后台。只做响应式 Web/PWA；真实支付方案未确定前，不开发自有余额沉淀。

### 阶段 C：公开测试（8–12 周）

加入评价体系、争议处理、作业合规审核、搜索推荐和小程序。支付调研完成后再接入持牌机构；根据真实供需继续扩展开发、数据、AI 视频、模型微调和算力租用。

## 9. 验证是否值得继续的指标

- 发布需求后 24 小时内获得有效初版报名的比例；
- 从发布到首份报名、从首份报名到选人的中位时间；
- 报名入选率、选人后按期提交率与一次验收通过率；
- 完成订单的复购率；
- 争议率、退款率和平台介入时长；
- 平台服务费收入能否覆盖获客、支付和人工仲裁成本。

最重要的不是注册数，而是 **7 天内完成真实结算的订单数**。

## 10. 已确定的三个产品决策

1. **主要推广需求：** 作业辅导与协作完成，但不限制其他合法类型的真实需求发布。
2. **订单获得方式：** 候选人先提交初版报名，发布方比较每个人的思路、小样、报价和时间后选定一人；进行中可以在订单内持续对话。
3. **支付阶段：** 暂不把平台担保作为当前开发前提，先跑通发布、报名、选人、沟通、交付、修改、验收和确认结算的完整流程。

以上三项已经同步进入交互原型。原型中的付款码、余额、充值和提现仍是本地演示；真实支付需要在完成流程验证后另行选择合规服务商和交易结构。
