# 算力集开发与提交规则

本文件适用于整个仓库。任何开发者、Codex 对话或自动化代理在修改文件前都必须阅读并遵守。

`AGENTS.md` 是开发工具自动识别的固定技术名称，因此保留英文文件名；内容、业务文件和可变协作记录尽量使用中文。

## 一、进入仓库后的必读顺序

1. `README.md`：项目总入口；
2. `.agents/项目入口.json`：机器可读的权威路径；
3. `文档/开发协作/导航.md`：开发文档分类导航；
4. `协作记录/任务进度/任务板.json`：任务状态、负责人和依赖；
5. 自己工作流对应的 `文档/开发协作/03-并行工作流/*.md`；
6. 涉及订单、报名、消息或结算时，继续阅读 `文档/开发协作/01-架构契约/领域与状态契约.md` 和 `接口契约规则.md`。

## 二、固定产品约束

- 主要推广“作业辅导与协作完成”，但允许其他合法的开发、数据、AI、设计和算力需求。
- 作业场景强调辅导、讲解、批改和共同完成；禁止代考、冒名提交、论文代写或绕过学校规则。
- 开放需求采用“多人提交初版报名，发布方比较后选定一人”，不得改回先到先得。
- 选人后开放订单沟通，然后进入交付、退回修改/验收、确认结算和完成。
- 当前不接入真实平台担保、充值或提现；付款码和余额只能明确标注为演示。

改变上述约束必须新增决策记录，由项目负责人确认后才能实施。

## 三、修改人和对话登记

1. 每个开发者或开发对话先在 `协作记录/人员与对话/人员登记表.md` 取得唯一编号，例如 `P-001`。
2. 每次任务必须记录：修改人编号、显示名称、对话/开发环境、分支、开始时间和当前状态。
3. Git 提交作者必须能对应到登记表；不能使用无法识别的公共作者名称。
4. 自动化代理应同时记录“工具名称”和“当前对话编号”，便于区分不同会话。

## 四、任务认领与并行开发

1. 从 `协作记录/任务进度/任务板.json` 选择“可开始”且依赖全部完成的任务。
2. 在 `协作记录/任务认领/` 新增 `认领-<任务号>-<修改人编号>.json`。
3. 一个修改人/对话同一时间最多认领一个“开发中”任务。
4. 只修改任务卡的 `负责路径`；需要触碰其他工作流文件时先停止并提交协调请求。
5. 两个有效认领不能覆盖相同文件或父子通配路径。
6. 推荐每个任务使用独立分支 `work/<任务号>-<简短英文标识>` 和独立 worktree。
7. 不覆盖、不重置、不清理其他人的未提交改动；发现路径冲突时停止写入并记录冲突。

当前 `app.js`、`index.html`、`styles.css` 属于同一个前端单体边界，同一时间只允许一个任务修改。

## 五、任务状态和强制顺序

状态顺序固定为：

`计划中 → 可开始 → 开发中 → 待核对 → 待确认 → 已确认 → 已完成`

异常状态为 `已阻塞` 或 `已取消`。

完成开发后不得直接标记“已完成”，必须依次执行：

1. 停止继续扩展范围，将任务改为“待核对”；
2. 按任务风险运行测试、静态检查、桌面/手机检查和安全检查；
3. 在 `协作记录/核对结果/` 新增核对报告；
4. 使用 `git diff` 和 `git status` 核对修改范围，只暂存已认领文件；
5. 创建实现提交，提交信息格式为 `[任务号][修改人编号] 中文摘要`；
6. 在 `协作记录/修改确认/确认记录/` 新增修改确认单，记录实现提交哈希；
7. 提交确认单，将任务改为“待确认”；
8. 项目负责人确认后改为“已确认”，合并后才改为“已完成”。

没有核对报告、实现提交和修改确认单的任务不得合并到 `main`。

## 六、修改确认单必须包含

- 任务号、修改人/对话编号、分支和日期；
- 修改前基线、实现提交哈希、确认记录提交哈希（可在提交后由 Git 历史获取）；
- 修改文件分类及原因；
- 完成的计划项和未完成项；
- 实际运行的检查、结果与证据路径；
- 风险、已知问题和生产环境未验证事项；
- 回滚命令或恢复步骤；
- 项目负责人确认状态。

## 七、工程约束

- 当前根目录是本地交互原型。生产代码逐步迁入 `src/client/`、`src/server/` 和 `src/shared/`，不要继续扩大根目录 `app.js`。
- 订单状态由服务端状态机决定；前端不能直接写任意状态。
- 金额使用整数“分”，账本只追加；资金回调必须具备幂等键。
- 报名选人必须在同一事务完成：锁定需求、选中一份报名、拒绝其他报名、创建协作关系、写审计事件。
- 生产订单消息必须服务端持久化并校验参与者身份，不能依赖 `localStorage`。
- 不提交密钥、真实身份证件、支付凭证、学生账号、课程平台密码或其他敏感数据。
- 新界面必须兼容桌面和 390px 手机视口，并保留键盘操作和可见焦点。
- 未经明确授权，不添加真实外部写操作、支付调用、短信发送或公开部署。

## 八、固定英文技术名称

以下名称因工具或 Web 标准保留：

- `AGENTS.md`、`.agents`、`README.md`；
- `.gitignore`、`.git`；
- `index.html`、`manifest.json`、`sw.js`；
- `app.js`、`styles.css`、`server.py`；
- 代码内部 API 路径、状态枚举和标准协议字段。

除这些固定名称外，新建的人类阅读文件优先使用清楚的中文目录和中文文件名。

## 九、完成标准

完整完成标准见 `文档/开发协作/00-治理规则/完成与提交标准.md`。最终报告必须区分：已验证、未验证、需要真实服务器/支付/目标设备才能验证的内容。
