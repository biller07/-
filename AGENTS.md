# 算力集仓库协作规则

本文件适用于整个仓库。任何开发者、自动化代理或新对话在修改文件前都必须遵守。

## 进入仓库后的必读顺序

1. 本文件；
2. `docs/development/README.md`；
3. `.agents/task-board.json`；
4. 自己工作流对应的 `docs/development/03-workstreams/*.md`；
5. 若涉及订单、报名、消息或结算，继续阅读 `docs/development/01-architecture/domain-contract.md` 和 `api-contract-rules.md`。

## 已冻结的产品决策

- 主要推广“作业辅导与协作完成”，但允许其他合法的开发、数据、AI、设计和算力需求。
- 作业场景必须强调辅导、讲解、批改和共同完成；禁止代考、冒名提交、论文代写或绕过学校规则。
- 开放需求采用“多人提交初版报名，发布方比较后选定一人”，不得改回先到先得的抢单模式。
- 选人后才开放订单对话，然后依次进入交付、退回修改/验收、确认结算和完成。
- 当前不接入真实平台担保、充值或提现。付款码和余额仅允许明确标注为演示。

变更这些决策必须新增决策记录，并由仓库协调者批准；普通工作流不得自行修改。

## 并行开发强制协议

1. 开始前在 `.agents/claims/` 新建认领文件，格式见该目录 README；一个人或一个对话同一时间最多持有一个 `in_progress` 任务。
2. 只修改任务卡中声明的 `ownedPaths`。需要触碰其他工作流文件时，先停止并写交接请求，不得顺手修改。
3. `app.js`、`index.html`、`styles.css` 当前属于同一个前端单体边界，同一时间只能有一个任务修改它们。
4. `domain-contract.md`、`api-contract-rules.md`、`.agents/workstreams.json` 和 `.agents/task-board.json` 是共享契约，仅协调者可直接修改。
5. 推荐每个开发者/对话使用独立 Git 分支和独立 worktree。若只能共享同一目录，文件认领仍是最低要求。
6. 不覆盖、不重置、不清理其他人的未提交改动；发现路径冲突时停止当前修改并记录冲突。
7. 完成时必须在 `docs/development/04-handoffs/` 写交接文件，列明改动、测试、已知问题和后续任务。

## 工程约束

- 当前根目录是本地交互原型。生产代码逐步迁入 `src/client/`、`src/server/` 和 `src/shared/`，不要继续扩大根目录 `app.js`。
- 订单状态由服务端状态机决定；前端不能直接写任意状态。
- 金额一律使用整数“分”，账本只追加；任何资金回调必须有幂等键。
- 报名选人必须在同一事务中完成：锁定需求、选中一份报名、拒绝其他报名、创建协作关系、写审计事件。
- 订单消息必须校验参与者身份，并持久化到服务端；生产版本不得只保存在 `localStorage`。
- 不提交密钥、真实身份证件、支付凭证、学生账号、课程平台密码或其他敏感数据。
- 新功能必须兼容桌面与 390px 手机视口，保留键盘可用性和可读的焦点状态。
- 未经明确任务授权，不添加真实外部写操作、支付调用、短信发送或公开部署。

## 验收底线

每个任务至少需要：

- 与改动风险相匹配的单元或集成测试；
- 涉及用户流程时，补充浏览器自动化；
- 桌面与手机视口检查；
- 无新增控制台错误；
- 文档与实现同步；
- 交接文件完整。

完整完成标准见 `docs/development/00-governance/definition-of-done.md`。
