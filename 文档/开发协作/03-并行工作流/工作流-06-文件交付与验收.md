# WS-06 文件、交付与验收

- 目标：实现安全的需求附件、交付版本、退回修改和验收历史。
- 拥有：`src/server/modules/files/**`、`src/server/modules/deliveries/**` 及专属测试。
- 依赖：WS-02、WS-03；对象存储接口契约由 WS-00 审核。
- 关键约束：私有桶、短时签名下载、参与者鉴权、大小/MIME/哈希/扫描状态；历史版本不可覆盖。
- 首版不做：公开分享链接、在线执行未知代码、自动打开 Office 宏。
- 验收：越权下载拒绝、恶意扩展名、超限文件、重复提交、退回再提交和历史版本测试通过。
