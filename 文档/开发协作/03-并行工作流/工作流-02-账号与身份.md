# WS-02 账号、会话与身份

- 目标：实现真实服务端用户、登录会话和最小资料，不含真实实名认证供应商。
- 拥有：`src/server/modules/auth/**`、`src/server/modules/users/**` 及专属测试。
- 依赖：WS-00 契约、服务端基础骨架。
- 交付：创建演示账号、登录/退出、当前用户、会话失效、权限中间件。
- 安全：密码使用成熟哈希；Cookie 为 HttpOnly/Secure/SameSite；登录限流；日志不含密码/令牌。
- 验收：跨用户隔离、会话过期、暴力尝试限制和未授权请求测试通过。
